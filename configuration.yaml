homeassistant:
  # Name of the location where Home Assistant is running
  name: Home
  # Location required to calculate the time the sun rises and sets
  latitude: 43.6403
  longitude: -79.3711
  # Impacts weather/sunrise data (altitude above sea level in meters)
  elevation: 176
  # metric for Metric, imperial for Imperial
  unit_system: metric
  # Pick yours from here: http://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  time_zone: America/Toronto
  # Duck DNS & Let's Encrypt
  internal_url: "http://192.168.77.13:8123"
  external_url: "http://q93jp6b.duckdns.org:8123"
  # Customization file
  customize: !include customize.yaml

#logger:
#  default: error
#  logs:
#    homeassistant.components.automation: debug
#    homeassistant.components.script: debug
#    homeassistant.core: debug
#    homeassistant.helpers.script: debug

# Configure a default setup of Home Assistant (frontend, api, etc)
default_config:
ios:

frontend:
  themes: !include_dir_merge_named themes

# menubar stuff
hassio:

# discovery service
discovery:

# device tracker UniFi
device_tracker: !include device_tracker.yaml

# Stream
stream:

# Nest
nest:
  client_id: 430378820674-89fulmsvv1lgb8rl3rkbsumn7h0v01c3.apps.googleusercontent.com
  client_secret: URkbQ-nfb9AoOjOda4uggrQW
  project_id: 9f01af0f-aed8-4b59-bd71-4ebe37e62c75
  subscriber_id: projects/deductive-cider-296321/subscriptions/KJFDYHKDM
#  subscriber_service_account: !include kjfdyhkdm-1605994376539-909486d0780e.json

# MyQ garage door
cover:
  - platform: myq
    username: !secret myq_user
    password: !secret myq_password
    type: liftmaster

# outdoor cameras
camera:
  - platform: mjpeg
    mjpeg_url: !secret backyard_mjpeg_url
    still_image_url: !secret backyard_still_image_url
    name: Backyard
    authentication: basic
    username: !secret backyard_camera_user
    password: !secret backyard_camera_password

  - platform: mjpeg
    mjpeg_url: !secret garage_mjpeg_url
    still_image_url: !secret garage_still_image_url
    name: Garage
    authentication: basic
    username: !secret garage_camera_user
    password: !secret garage_camera_password

# Speedtest
speedtestdotnet:

# System Health
system_health:

# HA includes
group: !include groups.yaml
automation: !include automations.yaml
script: !include scripts.yaml
sensor: !include sensors.yaml
light: !include light_group.yaml
scene: !include scenes.yaml
